name: Shadow Sync (Push from A)

on:
  workflow_dispatch:
  workflow_call:
    inputs:
      node-version:
        description: Node.js version to use on the runner.
        type: string
        default: '20'
    secrets:
      EXTERNAL_REPO_TOKEN:
        description: Fine-grained PAT with Contents Read+Write on the B-side repos.
        required: true

concurrency:
  group: shadow-sync-push
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ inputs.node-version || '20' }}

      - run: npm ci

      - name: Configure git
        run: |
          git config --global user.name "Shadow Sync"
          git config --global user.email "shadow-sync@users.noreply.github.com"
          git config --global url."https://x-access-token:${{ secrets.EXTERNAL_REPO_TOKEN }}@github.com/".insteadOf "https://github.com/"

      - name: Push from A into shadow branches on B
        run: npm run sync -- --from a
